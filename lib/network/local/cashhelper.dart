/*class cashhelp {
  static SharedPreterences sharedPreterences;
  static init() async {
    sharedPreterences = await sharedPreterences.getInstance();
  }
  static Future<bool> putData( {String key,bool value}) async {
     return await sharedPreterences.setBool(key,value);
    
  }
  static bool getData( {String key,})  {
       sharedPreterences.getBool(key);
    
  }

}*/